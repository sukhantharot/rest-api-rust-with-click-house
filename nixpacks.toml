# Railway Nixpacks configuration
[phases.build]
cmds = [
  "cargo build --release",
  "mkdir -p bin",
  "cp target/release/rarch bin/",
  "cp target/release/migrate bin/"
]

[phases.start]
cmd = "./bin/rarch"

[variables]
RUST_LOG = "info"